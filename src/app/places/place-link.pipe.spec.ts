import { PlaceLinkPipe } from './place-link.pipe';

describe('PlaceLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new PlaceLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
